server:
   port: 8888
spring:
   profiles:
      active: dev
   application:
      name: my-cloud-config-server
   cloud:
      config:
         server:
            git: 
               uri: E:\jenkins-workspace\cloud-config-repo